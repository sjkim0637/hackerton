# 해커톤 설계서 초안
## 카메라 기반 공간 편집 및 AR 가구 재배치 시뮬레이터

---

## 1. 프로젝트 한 줄 정의

스마트폰 카메라로 실제 공간을 촬영하고, 현실에 존재하는 가구를 **선택 → 제거 → 빈 공간 복원 → 다른 위치에 재배치**하거나, 새로운 가구 및 인테리어 요소를 실제 공간 위에 배치해 보는 **AR(증강현실) 기반 공간 편집 시스템**을 개발한다.

기존 AR 가구 배치 서비스가 주로 “없는 가구를 현실 공간에 추가해 보는 것”에 집중한다면, 본 프로젝트의 핵심 차별점은 **이미 존재하는 가구를 실제 공간에서 제거하고, 가려져 있던 공간을 자연스럽게 복원한 뒤 재배치하는 것**이다.

---

# 2. 원천 아이디어

사용자가 스마트폰 카메라로 집이나 방을 여러 방향에서 촬영한다.

촬영 과정에서 다음 정보를 확보한다.

- 카메라 영상
- 카메라 위치 및 방향
- 벽 / 바닥과 같은 평면 정보
- 주요 가구 및 사물 정보
- 사용자가 선택한 사물의 위치

전체 흐름은 다음과 같다.

```text
실제 공간 촬영
      ↓
카메라 위치 및 공간 정보 파악
      ↓
가구 / 사물 인식
      ↓
사용자가 사물 선택
      ↓
┌───────────────────────────┐
│ 기존 가구 이동             │
│ 기존 가구 제거             │
│ 새로운 가구 추가           │
│ 벽지 / 벽 색상 변경        │
└───────────────────────────┘
      ↓
AR 공간에서 결과 확인
```

예를 들어 TV가 설치된 벽을 촬영한 뒤 TV를 선택하고 삭제한다고 가정한다.

단순히 TV 영역의 픽셀만 지우면 아래처럼 빈 구멍이 생긴다.

```text
기존 화면

┌─────────────────────┐
│        벽           │
│     ┌─────────┐     │
│     │   TV    │     │
│     └─────────┘     │
└─────────────────────┘

단순 삭제

┌─────────────────────┐
│        벽           │
│     [ 빈 공간 ]     │
│                     │
└─────────────────────┘

목표 결과

┌─────────────────────┐
│                     │
│        벽           │
│                     │
└─────────────────────┘
```

따라서 핵심은 단순 사물 탐지가 아니라,

**사물 제거 후 가려져 있던 공간을 얼마나 자연스럽게 복원하고, 그 결과를 실제 공간 좌표에 얼마나 안정적으로 고정할 수 있는가**

이다.

---

# 3. 해커톤 핵심 기능

해커톤에서는 기능을 크게 두 가지 축으로 잡는다.

## 3.1 기존 가구 재배치

실제 카메라 영상 속 기존 가구를 선택하여 다음 작업을 수행한다.

- 가구 선택
- 기존 위치에서 제거
- 제거된 자리를 자연스럽게 복원
- 다른 위치로 이동
- 회전
- 크기 조절
- 벽 또는 바닥에 맞춰 배치

예시:

- TV를 다른 벽으로 옮긴다.
- 소파를 반대편으로 이동한다.
- 테이블을 창가 쪽으로 옮긴다.
- 기존 가구를 완전히 삭제한다.

---

## 3.2 새로운 가구 및 인테리어 요소 추가

현재 공간에는 없는 요소를 추가한다.

예시:

- 새로운 소파 배치
- 새로운 테이블 배치
- 새로운 TV 배치
- 벽지 변경
- 벽 색상 변경
- 바닥 재질 변경

이 기능은 추후 실제 가구 상품, 건설사 옵션, 인테리어 상품과 연결할 수 있다.

---

# 4. 해커톤 시연 시나리오

해커톤에서는 여러 기능을 얕게 보여주기보다 하나의 흐름을 완성도 있게 보여주는 것이 중요하다.

추천 시연 시나리오는 다음과 같다.

## 거실 재배치 시연

1. 사용자가 거실을 카메라로 촬영한다.
2. TV를 선택한다.
3. `삭제` 기능을 실행한다.
4. TV가 사라지고 뒤쪽 벽이 자연스럽게 복원된다.
5. 기존 TV를 다른 벽으로 이동한다.
6. 새로운 소파를 목록에서 선택한다.
7. 빈 공간에 새로운 소파를 배치한다.
8. 필요하면 벽지 또는 벽 색상을 변경한다.
9. 변경 전 / 변경 후 화면을 비교한다.

핵심 경험은 다음과 같다.

> “내 실제 집을 보면서 현실 공간 자체를 직접 편집한다.”

---

# 5. 전체 시스템 구조

```text
┌──────────────────────────────┐
│       스마트폰 애플리케이션   │
│                              │
│ 카메라 촬영                   │
│ AR 공간 추적                  │
│ 벽 / 바닥 인식               │
│ 사물 선택                    │
│ 가구 배치 및 렌더링          │
└───────────────┬──────────────┘
                │
         핵심 이미지 전송
         위치 / 마스크 정보
                │
                ▼
┌──────────────────────────────┐
│       중간 API 서버           │
│                              │
│ 사용자 작업 상태 관리         │
│ 이미지 저장                   │
│ 사물 정보 저장                │
│ AI 요청 중계                  │
│ 가구 데이터 관리              │
└───────────────┬──────────────┘
                │
         처리 방식 선택
                │
       ┌────────┼────────┐
       ▼        ▼        ▼
       A        B        C
   외부 AI   경량 모델   자체 엔진
```

중간 API 서버는 프로젝트의 핵심 기술은 아니다.

핵심 기술은 다음 두 영역이다.

1. **스마트폰에서 공간을 추적하고 결과를 안정적으로 배치하는 부분**
2. **기존 사물을 제거한 뒤 가려져 있던 공간을 복원하는 부분**

---

# 6. 아키텍처 방안 A — 외부 AI API 호출 방식

## 6.1 개념

가장 빠르게 높은 품질을 확보할 수 있는 방식이다.

사용자가 TV를 삭제하면 스마트폰에서 현재 장면의 대표 이미지를 서버로 전송하고, 서버가 외부 AI 이미지 생성 API에 다음과 같이 요청한다.

> “이 이미지에서 TV만 제거하고, TV 뒤에 원래 벽이 있었던 것처럼 자연스럽게 복원해줘.”

전체 흐름:

```text
카메라 촬영
   ↓
대표 이미지 추출
   ↓
사용자가 TV 선택
   ↓
TV 영역 표시
   ↓
중간 API 서버
   ↓
외부 AI 이미지 API
   ↓
TV 제거 + 벽 복원 이미지
   ↓
앱으로 결과 반환
   ↓
실제 벽 위치에 결과 적용
```

여기서 대표 이미지를 **키프레임(Keyframe, 특정 시점을 대표하는 기준 이미지)** 이라고 한다.

---

## 6.2 장점

- 구현 난이도가 낮다.
- 별도의 AI 학습이 필요하지 않다.
- 별도의 고성능 GPU 서버가 없어도 된다.
- 사물 제거 및 빈 공간 복원 품질이 상대적으로 높다.
- 해커톤에서 결과물을 빠르게 확보하기 좋다.

---

## 6.3 단점

- API 호출 비용이 발생한다.
- 인터넷 연결이 필요하다.
- 응답 시간이 발생한다.
- 외부 서비스 장애에 영향을 받는다.
- 같은 이미지를 여러 번 요청해도 조금씩 다른 결과가 나올 수 있다.
- 카메라를 움직였을 때 결과 이미지가 흔들리거나 어긋날 수 있다.

---

## 6.4 중요한 설계 원칙

AI API를 매 화면마다 호출하면 안 된다.

```text
매 프레임마다 API 호출
→ 사용 금지
```

다음과 같이 동작해야 한다.

```text
사용자가 실제 편집 명령 실행
        ↓
그 시점의 대표 이미지 저장
        ↓
AI API 한 번 호출
        ↓
결과 이미지 수신
        ↓
해당 결과를 실제 공간에 고정
```

즉 AI는 실시간 영상을 계속 만드는 것이 아니라,

**현실에서 카메라로 확인할 수 없었던 빈 공간을 한 번 복원하는 역할**

만 담당한다.

---

## 6.5 진짜 기술 난이도

외부 AI가 만들어 주는 결과는 기본적으로 2차원 이미지이다.

하지만 사용자가 스마트폰을 좌우로 움직이면 실제 공간은 보는 각도가 계속 달라진다.

따라서 AI가 만든 이미지를 단순히 화면에 붙이면 금방 티가 난다.

이를 해결하려면 AI가 생성한 벽 이미지를 실제 벽 위치에 고정해야 한다.

이를 **공간 고정(Spatial Anchoring, 생성 결과를 실제 공간 좌표에 고정하는 기술)** 이라고 한다.

또 카메라 각도가 변할 때 결과 이미지도 자연스럽게 보이도록 변환해야 한다.

이를 **재투영(Reprojection, 기존 이미지를 새로운 카메라 시점에 맞게 다시 계산하는 처리)** 이라고 한다.

해커톤에서 기술적으로 가장 중요한 부분 중 하나다.

---

# 7. 아키텍처 방안 B — 경량 모델 기반 처리

## 7.1 개념

외부 API를 사용하지 않고 스마트폰 또는 가까운 로컬 환경에서 작은 AI 모델을 직접 실행하는 방식이다.

```text
카메라
   ↓
사물 탐지
   ↓
사물 영역 분리
   ↓
경량 복원 모델
   ↓
빈 공간 복원
   ↓
AR 화면에 적용
```

필요 기능은 하나의 모델로 모두 처리하지 않고 나눌 수 있다.

- 사물 탐지
- 사물 영역 분리
- 거리 추정
- 빈 공간 복원

---

## 7.2 장점

- API 호출 비용이 없다.
- 인터넷이 없어도 동작할 수 있다.
- 응답 시간이 일정하다.
- 사용자 이미지가 외부 서버로 나가지 않아도 된다.
- 상품화 시 장기적인 운영 비용이 낮아질 수 있다.

---

## 7.3 단점

- 스마트폰 성능 제약이 있다.
- 메모리 사용량이 커질 수 있다.
- 발열 문제가 생길 수 있다.
- 배터리 소모가 커질 수 있다.
- 외부 대형 AI보다 복원 품질이 낮을 가능성이 크다.
- 모델별 모바일 최적화 작업이 필요하다.

특히 TV 뒤에 벽지가 아닌 복잡한 무늬, 선반, 가구 등이 존재하면 작은 모델이 자연스럽게 복원하지 못할 가능성이 높다.

따라서 해커톤에서는 B 방식을 메인으로 잡기보다 **A 방식과 비교하는 실험용 개발 흐름**으로 두는 것이 좋다.

---

# 8. 아키텍처 방안 C — 자체 공간 복원 엔진

## 8.1 개념

AI 이미지 생성에 최대한 의존하지 않고, 사용자가 촬영한 여러 장의 영상 정보를 이용하여 빈 공간을 직접 복원한다.

예를 들어 사용자가 TV 정면만 촬영한 것이 아니라 TV 왼쪽, 오른쪽, 위쪽 영역을 함께 촬영했다면 여러 화면에 존재하는 벽 정보를 모아서 TV 뒤쪽 영역을 추정한다.

```text
촬영 화면 1 ─┐
촬영 화면 2 ─┤
촬영 화면 3 ─┤
촬영 화면 4 ─┼→ 카메라 위치 계산
촬영 화면 5 ─┤        +
촬영 화면 6 ─┘   공간 구조 계산
                         ↓
                  주변 벽 정보 수집
                         ↓
                  가려진 영역 복원
```

사용 가능한 기술:

- 여러 화면 특징점 비교
- 카메라 이동량 계산
- 평면 기준 영상 변환
- 주변 무늬 복사
- 촬영 영상 누적
- 벽면 재구성

---

## 8.2 장점

- 성공할 경우 프로젝트의 강력한 원천 기술이 될 수 있다.
- 외부 AI API 비용이 없다.
- 외부 AI 서비스에 의존하지 않는다.
- 촬영된 실제 데이터를 중심으로 처리하기 때문에 결과가 일정하다.

---

## 8.3 단점

개발 난이도가 가장 높다.

가장 큰 근본적인 문제는 다음과 같다.

> 카메라가 한 번도 본 적 없는 영역의 실제 정보는 존재하지 않는다.

따라서 완전히 가려진 영역을 AI 생성 없이 완벽하게 복원하는 데는 한계가 있다.

예를 들어 TV 뒤쪽 벽에 액자가 있었는데 모든 촬영 화면에서 TV에 가려져 있었다면 자체 엔진은 그 액자의 존재를 알 수 없다.

---

# 9. A / B / C 개발 전략

세 방식 중 하나를 처음부터 확정하지 않는다.

```text
                 공간 편집 기능
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
          A            B            C
     외부 AI API    경량 모델    자체 엔진
          │            │            │
          └────────────┼────────────┘
                       ▼
                 결과 품질 비교
                       ↓
                   최종 선택
```

초기 우선순위는 다음과 같이 잡는다.

```text
A > B > C
```

즉,

1. A 방식으로 먼저 전체 기능이 실제로 동작하도록 만든다.
2. 이후 B 방식과 C 방식을 병렬로 실험한다.
3. B 또는 C에서 충분한 성능이 나오면 A의 일부 기능을 교체한다.

정리하면 다음과 같다.

> **A는 빠르게 완성품을 만들기 위한 방식이고, B와 C는 기술력을 높이기 위한 방식이다.**

---

# 10. 3인 분산 개발 구조

이번 프로젝트는 3명의 개발자가 각각 코딩 에이전트를 사용하여 동시에 개발한다.

초기 작업 영역은 다음과 같이 나눈다.

| 작업 흐름 | 초기 담당 | 주요 영역 |
|---|---|---|
| 공간 / AR 작업 흐름 | 사용자 1 | 카메라, 공간 추적, 벽/바닥 인식, 렌더링 |
| 영상 / AI 작업 흐름 | 사용자 2 | 사물 인식, 사물 영역 분리, 빈 공간 복원, AI API |
| 서버 / 통합 작업 흐름 | 사용자 3 | API 서버, 작업 상태, 가구 정보, 전체 연동 |

단, 이 구분은 담당자를 영구적으로 고정하기 위한 것이 아니다.

Redmine 또는 Jira처럼 **이슈 단위 할당 방식**으로 운영한다.

```text
할 일
 ↓
작업 가능
 ↓
담당자 지정
 ↓
작업 중
 ↓
검토
 ↓
병합 완료
```

자기 작업 흐름의 일이 끝났다면 다른 작업 흐름에서 선행 조건이 해결된 이슈를 가져가서 진행할 수 있다.

---

# 11. Git 브랜치 운영 방식

기본 브랜치:

```text
main
develop
```

각 사용자 작업 브랜치:

```text
agent/user1/*
agent/user2/*
agent/user3/*
```

예시:

```text
agent/user1/ar-plane-detection
agent/user1/spatial-anchor

agent/user2/object-mask
agent/user2/api-inpainting

agent/user3/session-api
agent/user3/asset-catalog
```

실험용 브랜치는 별도로 관리한다.

```text
experiment/mobile-inpainting
experiment/native-reconstruction
experiment/depth-estimation
```

실험 단계의 코드를 `develop` 브랜치에 바로 병합하지 않는다.

---

# 12. 작업 흐름 문서 구조

저장소에는 다음 문서를 둔다.

```text
/docs
    architecture.md
    roadmap.md

/workstreams
    spatial-ar.md
    vision-ai.md
    platform.md

/handoff
    user1.md
    user2.md
    user3.md

/issues
    backlog.md
    idea-backlog.md

/experiments
```

`workstream(작업 흐름 문서)`에는 최소 다음 내용을 작성한다.

```text
# 작업 흐름

## 목표

## 현재 구조

## 완료된 작업

## 진행 중인 작업

## 다음 작업

## API / 입력 / 출력

## 선행 조건

## 알려진 문제

## 결정 사항

## 실험 결과
```

---

# 13. Handoff 작성 규칙

`Handoff(작업 인수인계 문서)`는 매우 중요하다.

각 개발자와 에이전트는 작업이 끝날 때 반드시 자신의 Handoff 문서를 업데이트한다.

예시:

```text
# HANDOFF

담당자: User 2
브랜치: agent/user2/api-inpainting

## 완료
- 대표 이미지 업로드
- TV 영역 분리
- 외부 AI API 연동

## 현재 상태
TV 삭제 기능 동작 확인.

## 현재 문제
AI가 생성한 벽 무늬가 요청마다 조금씩 변경됨.

## 제공 기능
POST /scene/remove-object

입력:
- image
- mask
- object_type

출력:
- reconstructed_image

## 다음 작업
카메라 시점 변경 시 결과 이미지 정합 테스트.

## 연계 사항
User 1에서 복원된 이미지 영역을 실제 벽에 적용해야 함.
```

이렇게 해야 다른 개발자의 에이전트가 이전 대화 내용을 몰라도 문서만 읽고 바로 이어서 작업할 수 있다.

---

# 14. 상세 개발 로드맵

---

## PHASE 0 — 기본 설계 및 규칙 확정

**모든 작업의 선행 단계**

### 공통

- [ ] 해커톤 시연 시나리오 확정
- [ ] MVP(최소 기능 제품) 범위 확정
- [ ] 카메라 입력 형식 정의
- [ ] 대표 이미지 형식 정의
- [ ] 좌표계 정의
- [ ] 사물 ID 규칙 정의
- [ ] 사물 영역 표시 형식 정의
- [ ] 가구 데이터 형식 정의
- [ ] API 규격 정의
- [ ] 저장소 생성
- [ ] 브랜치 규칙 정의
- [ ] 작업 흐름 문서 생성
- [ ] Handoff 양식 생성
- [ ] 이슈 목록 생성

**PHASE 1 진행 전 반드시 완료**

---

# PHASE 1 — 전체 연결 구조 만들기

**PHASE 0 필요**

이 단계에서는 품질보다 처음부터 끝까지 기능이 한 번이라도 연결되는 것이 목표다.

---

## [병렬] 사용자 1 — 공간 / AR

- [ ] 카메라 화면 표시
- [ ] AR 실행 환경 구성
- [ ] 카메라 위치 및 방향 정보 획득
- [ ] 벽 / 바닥 평면 탐지
- [ ] 공간 기준점 생성
- [ ] 화면 터치 위치 획득
- [ ] 임시 가구 배치
- [ ] 가구 이동
- [ ] 가구 크기 조절
- [ ] 가구 회전
- [ ] 대표 이미지 캡처

---

## [병렬] 사용자 2 — 영상 / AI

- [ ] 테스트용 거실 이미지 선정
- [ ] 1차 대상 사물을 TV / 소파로 제한
- [ ] 사물 영역 분리 방식 실험
- [ ] 외부 AI 이미지 복원 API 시험
- [ ] TV 삭제 요청 문구 실험
- [ ] 소파 삭제 요청 문구 실험
- [ ] 결과 이미지 저장
- [ ] 평균 응답 시간 측정
- [ ] 호출 비용 기록

---

## [병렬] 사용자 3 — 서버 / 통합

- [ ] API 서버 기본 구조 생성
- [ ] 사용자 작업 세션 생성
- [ ] 이미지 업로드 기능
- [ ] 대표 이미지 저장
- [ ] 사물 정보 저장
- [ ] 외부 AI 연결 구조 생성
- [ ] 가구 데이터 API 생성
- [ ] 오류 처리
- [ ] 요청 로그 기록

---

## PHASE 1 완료 조건

다음 흐름이 한 번이라도 성공하면 완료한다.

```text
카메라
→ 대표 이미지
→ 서버
→ AI 처리
→ 변경 이미지
→ 스마트폰
```

이 단계에서는 결과가 다소 어색해도 상관없다.

---

# PHASE 2 — 기존 사물 삭제 기능

**PHASE 1 필요**

---

## 사용자 1

- [ ] 사물 선택 화면
- [ ] 선택 영역 표시
- [ ] 삭제 버튼
- [ ] 복원 결과 이미지 수신
- [ ] 결과 이미지를 벽면에 적용
- [ ] 삭제 전 / 삭제 후 전환

---

## 사용자 2

- [ ] 사물 영역 분리 개선
- [ ] 사물 경계 보정
- [ ] AI 요청 문구 개선
- [ ] 그림자 제거 시험
- [ ] 경계 깨짐 확인
- [ ] TV 테스트
- [ ] 소파 테스트
- [ ] 테이블 테스트
- [ ] 품질 비교용 이미지 모음 작성

---

## 사용자 3

- [ ] `/scene/remove-object` API
- [ ] 대표 이미지 버전 관리
- [ ] 처리 결과 임시 저장
- [ ] AI 호출 횟수 기록
- [ ] 비용 기록
- [ ] 처리 시간 초과 대응
- [ ] 동일 요청 중복 호출 방지
- [ ] 실패 시 재시도 정책

---

# PHASE 3 — 공간 정합 및 카메라 이동 대응

**PHASE 2 필요**

이 단계가 프로젝트의 기술적 핵심이다.

외부 AI API를 쓰더라도 이 기능이 없으면 단순 이미지 편집 서비스와 차이가 없다.

---

## 사용자 1 중심

- [ ] AI 결과 이미지를 실제 공간 좌표와 연결
- [ ] 벽 평면 기준 좌표 변환
- [ ] 카메라 각도 변화에 따른 화면 보정
- [ ] 카메라 이동 테스트
- [ ] 복원 이미지를 공간 기준점에 고정
- [ ] 앞뒤 가림 처리
- [ ] 화면 흔들림 최소화
- [ ] 경계 자연스럽게 섞기

---

## 사용자 2 지원

- [ ] AI 결과에서 실제 변경 영역 추출
- [ ] 경계 부드럽게 처리
- [ ] 기존 벽 색상과 생성 결과 색상 맞추기
- [ ] 밝기 차이 보정
- [ ] 이상 결과 감지

---

## 사용자 3 지원

- [ ] 복원 결과 버전 관리
- [ ] 결과 이미지 임시 저장
- [ ] 스마트폰 전달 최적화

이 단계는 병렬 개발이 가능하지만 사용자 2와 사용자 3 작업 일부는 사용자 1의 공간 좌표 처리 방식에 맞춰야 한다.

---

# PHASE 4 — 기존 가구 재배치

**PHASE 2 완료 필요 / PHASE 3 일부 필요**

---

## 사용자 1

- [ ] 기존 가구 위치 저장
- [ ] 기존 위치에서 제거
- [ ] 가구 위치 이동
- [ ] 손가락으로 끌어서 이동
- [ ] 가구 회전
- [ ] 가구 크기 조절
- [ ] 벽 자동 맞춤
- [ ] 바닥 자동 맞춤
- [ ] 기본 충돌 처리

---

## 사용자 2

- [ ] 기존 가구 이미지 분리
- [ ] 가구 앞쪽 영역 추출
- [ ] 가구 외곽선 보정
- [ ] 필요 시 2D 이미지를 배치 가능한 형태로 변환

---

## 사용자 3

- [ ] 공간 상태 저장
- [ ] 가구 위치 / 회전 / 크기 저장
- [ ] 실행 취소 / 다시 실행 상태 관리
- [ ] 공간 버전 관리

---

# PHASE 5 — 새로운 가구 및 인테리어 요소 추가

**PHASE 1 이후 병렬 진행 가능**

PHASE 2~4와 독립적으로 개발할 수 있다.

---

## 사용자 1

- [ ] 3D 가구 배치
- [ ] 크기 조절
- [ ] 회전
- [ ] 바닥 자동 맞춤
- [ ] 벽 자동 맞춤
- [ ] 가구 선택 화면
- [ ] 가구 삭제

---

## 사용자 2

- [ ] 벽 영역 인식
- [ ] 바닥 영역 인식
- [ ] 벽지 변경 시험
- [ ] 벽 색상 변경 시험
- [ ] 바닥 재질 변경 시험

---

## 사용자 3

- [ ] 가구 목록 API
- [ ] 가구 정보 관리
- [ ] 미리보기 이미지
- [ ] 가구 실제 크기 정보
- [ ] 카테고리
- [ ] 가구 파일 내려받기
- [ ] 캐시 처리

---

# PHASE 6 — 아키텍처 B 경량 모델 실험

**PHASE 1 이후 병렬 가능**

- [ ] 스마트폰에서 실행 가능한 모델 조사
- [ ] 모델 파일 크기 기록
- [ ] 메모리 사용량 측정
- [ ] 처리 시간 측정
- [ ] 사물 영역 분리 품질 측정
- [ ] 빈 공간 복원 품질 측정
- [ ] 스마트폰 발열 확인
- [ ] 배터리 영향 확인
- [ ] 화면 처리 속도 영향 확인
- [ ] 외부 AI 방식과 품질 비교
- [ ] 외부 AI 방식과 비용 비교

비교 기준:

```text
결과 품질
응답 시간
비용
스마트폰 부하
인터넷 의존성
개발 난이도
```

---

# PHASE 7 — 아키텍처 C 자체 엔진 실험

**PHASE 1 이후 병렬 가능 / 추가 목표**

- [ ] 이전 촬영 화면 보관
- [ ] 각 화면의 카메라 위치 저장
- [ ] 화면 간 동일 특징점 찾기
- [ ] 여러 화면 정렬
- [ ] 벽 평면 정보 추출
- [ ] 주변 벽 무늬 수집
- [ ] 주변 무늬를 이용한 빈 영역 채우기
- [ ] 카메라 시점에 맞는 영상 변환
- [ ] 작은 구멍 자동 채우기
- [ ] 외부 AI 방식과 품질 비교

C 방식이 일부라도 성공하면 발표에서 강력한 기술 요소로 활용할 수 있다.

예:

> “외부 생성 AI뿐 아니라 기존 촬영 영상 자체를 이용한 공간 복원 방식도 병렬로 검증했다.”

---

# PHASE 8 — 속도 / 비용 / 안정성 개선

**PHASE 3~5 필요**

- [ ] 대표 이미지 선택 방식 개선
- [ ] 불필요한 AI API 호출 제거
- [ ] 동일 결과 재사용
- [ ] 이미지 용량 줄이기
- [ ] 필요한 영역만 잘라서 전송
- [ ] 필요한 사물 영역만 서버로 전송하는 방식 실험
- [ ] 전체 응답 시간 기록
- [ ] API 비용 기록 화면
- [ ] 하루 최대 API 사용량 제한
- [ ] 사용자 작업당 최대 호출 횟수 제한
- [ ] 실패 시 재시도
- [ ] 외부 API 장애 시 대체 처리

중요 원칙:

```text
매 화면마다 API 호출
X

사용자가 실제 편집 명령을 실행할 때만 호출
O
```

---

# PHASE 9 — 해커톤 시연 완성

- [ ] 거실 시연 환경 확정
- [ ] TV 삭제
- [ ] 삭제한 위치 벽 복원
- [ ] TV 재배치
- [ ] 새로운 소파 추가
- [ ] 벽지 또는 벽 색상 변경
- [ ] 변경 전 / 변경 후 화면
- [ ] AI 처리 중 화면
- [ ] 오류 발생 화면
- [ ] 이동 애니메이션
- [ ] 결과 저장
- [ ] 시연 영상 녹화
- [ ] 발표 순서 작성
- [ ] 실제 시연 실패 대비 녹화 영상 준비

---

# 15. 전체 선행 관계

```text
                 PHASE 0
                    │
                 PHASE 1
                    │
       ┌────────────┼──────────────┐
       │            │              │
       ▼            ▼              ▼
    PHASE 2      PHASE 5        PHASE 6
       │        병렬 가능        B 실험
       │
       ├──────────────────┐
       ▼                  ▼
    PHASE 3            PHASE 7
  공간 정합             C 실험
       │
       ▼
    PHASE 4
  가구 재배치
       │
       └────────┬─────────┘
                ▼
             PHASE 8
                │
                ▼
             PHASE 9
```

중요한 것은 세 명이 같은 PHASE가 끝날 때까지 기다리지 않는 것이다.

자기 작업이 끝난 개발자는 **선행 조건이 해결된 다른 이슈를 바로 가져가서 작업**한다.

---

# 16. 기술 스택 초안

| 영역 | 기술 후보 |
|---|---|
| 스마트폰 앱 | Android / Kotlin |
| AR 기능 | ARCore |
| 화면 렌더링 | ARCore 기반 렌더링 |
| 영상 처리 | OpenCV |
| 서버 | FastAPI |
| 통신 | REST API |
| 이미지 형식 | JPEG / PNG / WebP |
| 외부 AI | 이미지 생성 / 이미지 편집 API |
| 경량 AI | ONNX Runtime 또는 TensorFlow Lite |
| 3D 가구 | glTF / GLB |
| 데이터 형식 | JSON |
| 데이터 저장 | SQLite 또는 PostgreSQL |
| 소스 관리 | GitHub |
| 개발 에이전트 | Codex / Claude Code 등 |
| 문서 | Markdown |

해커톤 단계에서는 서버와 데이터베이스에 과도하게 시간을 쓰지 않는다.

---

# 17. 주요 제한 및 제약사항

## 17.1 LiDAR 미사용

스마트폰의 일반 카메라를 기준으로 개발한다.

따라서 LiDAR 센서가 없는 일반 안드로이드 기기에서도 동작하는 방향을 기본으로 한다.

---

## 17.2 가려진 공간 문제

가구 뒤쪽은 카메라가 한 번도 본 적 없는 공간일 수 있다.

따라서 완벽한 복원을 하려면 결국 생성 또는 추론이 필요할 수 있다.

---

## 17.3 외부 AI API 비용

API 호출 횟수에 따라 비용이 발생한다.

따라서 사용자가 실제로 편집 기능을 실행하는 순간에만 호출한다.

API 사용량 제한 기능도 반드시 고려한다.

---

## 17.4 응답 시간

외부 AI 요청을 보내고 결과를 받을 때까지 시간이 필요하다.

그 사이에도 사용자는 스마트폰을 움직일 수 있다.

따라서 요청 당시의 다음 정보를 반드시 저장한다.

- 대표 이미지
- 카메라 위치
- 카메라 방향
- 벽 / 바닥 정보
- 공간 기준점
- 사물 영역

AI 결과가 돌아왔을 때 현재 화면 기준으로 적용하면 안 된다.

**요청 당시의 실제 공간 위치 기준으로 다시 적용해야 한다.**

---

## 17.5 화면 간 일관성

한 장의 이미지를 예쁘게 만드는 것보다 더 어려운 문제다.

사용자가 스마트폰을 움직여도 복원된 벽이 실제 벽에 붙어 있는 것처럼 보여야 한다.

즉,

> **“AI가 TV를 얼마나 예쁘게 지웠는가”보다 “사용자가 이동해도 지운 자리가 흔들리지 않는가”가 더 중요하다.**

이 부분이 프로젝트의 핵심 기술 요소가 될 가능성이 높다.

---

# 18. 개발 중 아이디어 추가 원칙

이번 프로젝트는 처음부터 모든 기능을 확정하지 않는다.

개발하면서 재미있는 아이디어 또는 기술적 개선점이 생각나면 적극적으로 추가한다.

다만 현재 개발을 중단하지 않고 다음 파일에 기록한다.

```text
/issues/idea-backlog.md
```

예시:

- 가구를 지우면 기존 그림자도 함께 제거
- 새로운 가구 배치 시 간단한 그림자 생성
- 가구를 벽 가까이 가져가면 자동 맞춤
- 실제 방 크기에 맞춰 가구 크기 자동 조정
- 가구가 벽을 뚫으면 경고
- “TV 없애줘” 같은 음성 명령
- 변경 전 / 변경 후 비교 막대
- 한 번에 실행 취소
- 방 전체 초기화
- 여러 배치안 저장
- 배치안 A / B 비교
- 빈 공간 감지 후 가구 추천
- 실제 가구 크기 자동 측정
- 문이 열리는 범위에는 가구 배치 금지
- 콘센트를 가리는 위치에 가구 배치 시 경고
- 사용자가 많이 보는 시점 자동 저장
- 결과를 사진 또는 영상으로 내보내기
- 가족에게 배치안 공유
- 가구 배치 투표 기능
- 실제 판매 상품 연결

이런 아이디어는 해커톤 핵심 기능을 방해하지 않는 범위에서 추가한다.

---

# 19. 모든 개발 에이전트 공통 지시사항

현재 프로젝트는 **3명의 개발자와 각 개발자의 코딩 에이전트가 동시에 개발하는 병렬 프로젝트**다.

각 에이전트는 다음 규칙을 반드시 따른다.

1. 본인에게 지정된 작업 흐름을 우선 수행한다.
2. 하지만 본인 작업 영역에만 고정되지 않는다.
3. 담당 이슈가 끝나면 선행 조건이 해결된 다른 이슈를 가져와 작업할 수 있다.
4. 다른 영역을 수정하기 전에 `/workstreams`와 `/handoff` 문서를 먼저 확인한다.
5. 작업 시작 전에 현재 브랜치와 관련 이슈를 확인한다.
6. 하나의 큰 기능을 한 번에 개발하지 말고 작은 이슈 단위로 나눈다.
7. 작업 완료 시 코드만 저장하지 말고 반드시 Handoff 문서를 갱신한다.
8. 다른 에이전트가 이전 대화 내용을 몰라도 이어서 작업할 수 있도록 입력값, 출력값, 현재 문제, 선행 조건을 문서에 남긴다.
9. 실험적인 기능은 `experiment/*` 브랜치에서 진행한다.
10. 검증되지 않은 실험 코드를 `develop` 브랜치에 바로 병합하지 않는다.
11. 새로운 아이디어가 떠오르면 현재 작업을 중단하지 말고 `idea-backlog.md`에 먼저 기록한다.
12. 실제 동작하지 않는 구조를 오래 설계하기보다 작은 기능이라도 먼저 실행 가능한 상태를 만든다.

가장 중요한 개발 원칙은 다음과 같다.

> **기술적으로 가장 멋진 방법을 먼저 만들지 않는다.  
> 먼저 처음부터 끝까지 실제로 동작하는 기준 구현을 확보한다.  
> 이후 동작하는 구조를 유지하면서 더 높은 기술 방식으로 하나씩 교체한다.**

따라서 기본 개발 전략은 다음과 같다.

```text
A 방식으로 전체 기능 완성
        ↓
B / C 방식 병렬 실험
        ↓
성능이 좋은 부분만 교체
        ↓
최종 해커톤 결과물 완성
```

---

# 20. 최종 목표

해커톤에서 단순히 “AR로 가구를 놓아봤다”가 아니라 다음 경험을 보여주는 것이 목표다.

> **현실 공간을 촬영하고, 기존 현실을 지우고, 옮기고, 다시 구성하는 공간 편집 도구**

장기적으로는 다음 방향으로 확장할 수 있다.

- 가구 구매 전 실제 공간 시뮬레이션
- 건설사 옵션 상품 체험
- 입주 전 인테리어 시뮬레이션
- 기존 가구 재배치 추천
- 실제 가구 카탈로그 연결
- 인테리어 업체 연계
- 집 구조 변경 시뮬레이션
- 공간 리모델링 사전 검토

하지만 해커톤에서는 기능을 과도하게 넓히지 않고,

**“기존 가구를 지우고 → 빈 공간을 복원하고 → 다시 옮긴다.”**

이 핵심 경험을 가장 높은 완성도로 구현하는 것을 우선한다.
