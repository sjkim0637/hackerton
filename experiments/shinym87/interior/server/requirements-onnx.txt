# onnxruntime 기반 로컬 AI 기능(MobileSAM 점 프롬프트 세그멘테이션, LaMa 인페인팅)을
# 실제로 켤 때 필요하다. 관련 경로(INTERIOR_MOBILESAM_ENCODER_PATH 등,
# INTERIOR_LAMA_MODEL_PATH)를 설정하지 않으면 app/ai/mobilesam.py, app/ai/lama.py 는
# onnxruntime 을 import 하지 않으므로(지연 import) 이 파일 없이도 서버가 뜬다.
# 단, INTERIOR_AI_PROVIDER 기본값이 lama 이므로 실사용(mock 아닌 진짜 인페인팅)에는
# 사실상 필수다.
#
# 설치: pip install -r requirements.txt -r requirements-onnx.txt
onnxruntime>=1.17
