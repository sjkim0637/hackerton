# MobileSAM 점 프롬프트 세그멘테이션을 실제로 켤 때만 필요하다.
# INTERIOR_MOBILESAM_ENCODER_PATH / INTERIOR_MOBILESAM_DECODER_PATH 를 설정하지 않으면
# app/ai/mobilesam.py 는 onnxruntime 을 import 하지 않으므로(지연 import) 이 파일 없이도 서버가 뜬다.
#
# 설치: pip install -r requirements.txt -r requirements-mobilesam.txt
onnxruntime>=1.17
