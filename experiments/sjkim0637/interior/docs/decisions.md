# 결정 기록

## D1 — 추천 거실 시연 시나리오 채택

- 상태: Accepted
- 결정: TV 삭제·벽 복원·TV 재배치·새 소파 배치·전후 비교 흐름을 초기 시연으로 사용한다.
- 이유: 핵심 차별점인 기존 사물 제거와 AR 재배치를 하나의 짧은 흐름에서 보여줄 수 있다.
- 변경 가능성: 실기기 검증과 해커톤 발표 구성에 따라 확장하거나 변경할 수 있다.

## D2 — 초기 MVP 범위 고정

- 상태: Accepted
- 결정: `phase-0.md`의 포함·제외 범위를 초기 기준으로 사용한다.
- 이유: 전체 흐름을 먼저 완성하고 자동 인식, 고품질 3D, 정교한 그림자 같은 고난도 기능은 후순위로 두기 위해서다.

## D3 — 외부 AI 방식 A 우선

- 상태: Accepted
- 결정: PHASE 1은 외부 AI 이미지 편집 API를 우선 사용한다.
- 이유: 자체 모델 학습 없이 짧은 기간에 TV 제거와 벽 복원 품질을 확인할 수 있다.
- 영향: 네트워크, API 비용, 응답 시간과 Provider 장애를 관리해야 한다.

## D4 — bbox 우선

- 상태: Accepted
- 결정: 초기 사물 영역은 정규화 bbox로 지정한다.
- 이유: 정밀 segmentation보다 앱과 서버 연결을 빠르게 검증할 수 있다.
- 이후: PHASE 2에서 polygon 또는 mask로 개선한다.

## D5 — Mock 우선 연결

- 상태: Accepted
- 결정: 실제 외부 AI를 붙이기 전에 Mock AI로 전체 E2E를 통과시킨다.
- 이유: 앱·서버·네트워크 문제와 AI Provider 문제를 분리해 진단하기 위해서다.

## D6 — 개인 독립 경로 사용

- 상태: Accepted
- 결정: Branch는 `agent/sjkim0637/interior`, 프로젝트 경로는 `experiments/sjkim0637/interior/`를 사용한다.
- 이유: 다른 사용자의 구현과 충돌 없이 독립적으로 개발하고 나중에 선택적으로 통합하기 위해서다.
